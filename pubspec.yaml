name: taller_flutter_web
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0-308.0.dev

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^4.0.4 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

